/**
 * Context for the tasks.
 *
 * @file context.js
 * @description Context for the tasks.
 * @module App
 */

import { createContext } from 'react';

/**
 * Context for the tasks.
 *
 * @type { Object }
 */
export const TasksContext = createContext( undefined );
